import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    background: {
      default: `#000000`,
      paper: `#333333`,
    },
    text: {
      primary: `#ffffff`,
      secondary: `#757575`,
    },
  },
});
